import { OrdersDirective } from './orders.directive';

describe('OrdersDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdersDirective();
    expect(directive).toBeTruthy();
  });
});
